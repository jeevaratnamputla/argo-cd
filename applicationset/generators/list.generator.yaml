apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: list-applicationset
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: list-applicationset-app
            repoURL: https://github.com/jeevaratnamputla/argo-cd          #pod
            targetRevision: testcase
            path: source-data/application-set-data/list-data/app1
  template:
    metadata:
      name: '{{name}}'
      labels:
        for: jpmc
    spec:
      project: default
      source:
        repoURL: '{{repoURL}}'
        targetRevision: '{{targetRevision}}'
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{name}}-namespace'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
